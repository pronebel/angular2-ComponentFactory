/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ComponentBuilderDirective } from './component-builder.directive';

describe('Directive: ComponentBuilder', () => {
  it('should create an instance', () => {
    let directive = new ComponentBuilderDirective();
    expect(directive).toBeTruthy();
  });
});
